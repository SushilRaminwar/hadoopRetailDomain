. ./Retail.properties
echo "$RetailMROutputpath"
pig -e "A = LOAD '/"$RetailMROutputpath"/HighBuzzProduct-r-00000' using PigStorage('\t') as (RTLID:int,RTLName:chararray,TypeOfCrawling:chararray,Prod_URL:chararray,Title:chararray,SalePrice:float,egPrice:float,RebatePercent:int,StockInfo:chararray); desA = distinct A ;odrA = order desA By pid; store odrA DESC into '/"$retailPigOutput"/HBP';"
pig -e "B = LOAD '/"$RetailMROutputpath"/NormalProduct-r-00000' using PigStorage('\t') as (RTLID:int,RTLName:chararray,TypeOfCrawling:chararray,Prod_URL:chararray,Title:chararray,SalePrice:float,egPrice:float,RebatePercent:int,StockInfo:chararray); desB = distinct B; odrB = order desB By pid; store odrB DESC into '/"$retailPigOutput"/NP';"
pig -e "C = LOAD '/"$RetailMROutputpath"/RareProduct-r-00000' using PigStorage('\t') as (RTLID:int,RTLName:chararray,TypeOfCrawling:chararray,Prod_URL:chararray,Title:chararray,SalePrice:float,egPrice:float,RebatePercent:int,StockInfo:chararray); desC = distinct C ;odrC = order desC By pid ;store odrC DESC into '/"$retailPigOutput"/RP';"
pig -e "D = LOAD '/"$RetailMROutputpath"/OnDemandCrawlProduct-r-00000' using PigStorage('\t') as (RTLID:int,RTLName:chararray,TypeOfCrawling:chararray,Prod_URL:chararray,Title:chararray,SalePrice:float,egPrice:float,RebatePercent:int,StockInfo:chararray); desD = distinct D ;odrD = order desD By pid; store odrD DESC into '/"$retailPigOutput"/ODCP';"
pig -e "E = LOAD '/"$RetailMROutputpath"/AvailableProduct-r-00000' using PigStorage('\t') as (RTLID:int,RTLName:chararray,TypeOfCrawling:chararray,Prod_URL:chararray,Title:chararray,SalePrice:float,egPrice:float,RebatePercent:int,StockInfo:chararray); desE = distinct E; odrE = order desE By pid; store odrE DESC into '/"$retailPigOutput"/AP';"
pig -e "F = LOAD '/"$RetailMROutputpath"/OtherProducts-r-00000' using PigStorage('\t') as (RTLID:int,RTLName:chararray,TypeOfCrawling:chararray,Prod_URL:chararray,Title:chararray,SalePrice:float,egPrice:float,RebatePercent:int,StockInfo:chararray); desF = distinct F; odrF = order desF By pid; store odrF DESC into '/"$retailPigOutput"/OP';"